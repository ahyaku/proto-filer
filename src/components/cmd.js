'use strict';

import React from 'react';

const Cmd = () => {
  const style = {
    minHeight: '20px',
    height: '20px',
    border: '1px solid #FFFFFF',
    overflowX: 'hidden',
    overflowY: 'hidden'
  };
  return (
    <div style={style}>
      Cmd
    </div>
  );
}

export default Cmd;

